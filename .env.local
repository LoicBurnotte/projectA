# Run projectA standalone at /about (same as when mounted in the host).
# Copy to .env.local and run: npm run start — then open http://localhost:3001/about
VITE_BASENAME=/about

# Host URL (for loading host/Header, host/config). Only needed when host is not local.
# VITE_HOST_URL=https://your-host.vercel.app

VITE_HOST_URL=https://host-five-liard.vercel.app/
# VITE_HOST_URL=http://localhost:3000/build